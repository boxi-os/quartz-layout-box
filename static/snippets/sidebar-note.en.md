This handbook belongs to the Quartz plugin **quartz-layout-box**. And it is built with
it: this box *is* a layout box.

- <a href="{{root}}/en/1-getting-started/">Getting started</a> — your first box in five minutes
- <a href="{{root}}/en/3-placement/">Placement</a> — where a box can sit
- <a href="{{root}}/en/8-recipes/">Recipes</a> — finished examples to copy
- <a href="{{root}}/en/9-reference/">Reference</a> — every option on one page

It comes from a *Markdown file*, not from the configuration — which way is better when
is in <a href="{{root}}/en/2-the-snippet/">The snippet</a>.
